
-- Personal Identity Group

Stop providing inconsistent information.

Make it reusable and modularized.

Ensure that when it is referenced, it reflects immediately everywhere.

Changes in one place must reflect globally.

Must be unique and identical for any and every location.

Must be the same for accused, victim, and witness with no differences at all.

"Anywhere" means anywhere; "everywhere" means everywhere.

Implement in all forms where referenced.

Implement in all steps where referenced.

Change the group in one place, and it updates everywhere.

-- Style

Style differently per role via the CSS class; that is proper modularization.

Stop providing inconsistent information.



-- Placeholder

Placeholder values should mention English or Bengali fields in brackets.

Placeholder value example: শেষ নাম (ইংরেজিতে)


--modularise the এফআইআর/জিডি দায়ের হয়েছে? *
না (No)
পুলিশ নিতে অস্বীকার (Police Refused)
হ্যাঁ (Yes
name it law-gd-fir something similar. it should follow modular and reusable logic and rules.
inside gd or fir there is another module thana search option. remember this

--Attachement 
Attachment option should be modularised and reusable. except label, hints and css styles are different.
Location Fields Standard setting


--Location Map Search Option should be separate and modularised

--Location Thana Search option should have separate modularise module
Used in the GD/Fir radio button when Yes selected. 


-- Social Link should be modularised and reusable 


-- Database Implementation

You must understand that all the group including the Name Group 
and Personal Identity Group require an identical data entry protocol.

Field mapping from UI fields to database columns must be uniform 
across all instances.

The data entry order and the format within the database must be identical.

All data must be processed into the same table using the same data entry operations.

This modular and reusable framework is not limited to UI changes; 
it is a standardized, modularized data entry format for the backend.

Is this requirement clear?

The same standards apply to Party Details whenever they are referenced.

This logic must be applied to any modularized, reusable groups without exception.

Consistency must be maintained "anywhere and everywhere," across every form and every step.

Have you strictly followed the rules mentioned above in your implementation?


-- Documents

Update all changes and field mapping documents.

-- Collectstatic

Stop providing inconsistent information.


Recommended UI/UX for this Context:
Low-Data Mode: Make the site "light" so it loads on a slow 3G/4G connection.

Progressive Web App (PWA): Let users "Install" the website on their phone home screen.
so it feels like an app.

No Login Required: Bangladeshi users often drop off if they have to create an account. 
Make the tools "One-Click."


maybe label and options put it in one line rather than two line. 
but make it responsive for mobile or smaller screen devices. 

--album maker
I want to create a digital layout inspired by physical photo albums 
that features three side-by-side frames in a standard 5"x7" or 7"x5" 
orientation on a single page. there will also other kind of size orientation.
These three frames will be displayed as dotted-line boxes where users can drag and drop their images, 
with a built-in feature to easily interchange picture positions between the boxes.

📶 ইন্টারনেট স্পিড মিটার (Speed Test):

কাজ: ব্যবহারকারী তার বর্তমান ডাটা স্পিড কত তা পরীক্ষা করতে পারবে।

📅 বয়স ক্যালকুলেটর (Age Calculator):

কাজ: চাকরির ফরমে অনেক সময় "অমুক তারিখে বয়স কত দিন, মাস, বছর" তা জানতে চায়, এটি দ্রুত সেই হিসাব করে দেবে।




