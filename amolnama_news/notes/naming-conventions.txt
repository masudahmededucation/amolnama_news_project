
--naming conventions
check name rules
seo friendly name 
check variables, filename, functions, features.
all files css, js, ajax, html
use large descriptive name always.
do not use partial name or short name.
update documents
update troubleshooting documents
don't make file too large which would be harder to manage and modify.
split larger files into smaller unit and files.
split files when the feature is atomic and independent.
don't split inter-related or co-related jobs.
check name rules

do you think we should use prefix to name table? 
for example: fact_check for fact check tables?
for ref table ref_fact_check as part of the table name. 
as a result to identify group of table for fact check? for all other group of works. 
should we rename our table like that?


--drop old tables + create new tables
drop the old tables, create new table to follow this rules?
what do you think?
do you think we should use prefix to name table? 
for example: fact_check for fact check tables?
for ref table ref_fact_check as part of the table name. 
as a result to identify group of table for fact check? for all other group of works. 
should we rename our table like that?
--en or _bn name
remember we need _en or _bn for important name column 
or ref table column where we need sorting.
everywhere else general names.

i think if any changes need to happen. 
you should give me db script first. 
then you start working.
you see working group by group makes it easier. 
you should make it rule. 
divide works in small chunks and make progress.

--test
check table and column names are matching specially id column names.
check sql queries including, report, select, insert, update, delete