Session 2026-04-07 — Code Quality Audit + Background Task Wiring
=================================================================

SCOPE: Full project audit against ALL memory rules + newsengine verification.

================================================================================
ISSUE 1: Dead Code — _run_background_task (post/views_api.py)
================================================================================

Problem: Helper function created but NEVER called. 15 raw threading.Thread calls
         remained across post/views_api.py (10) and debate/views_api.py (5).

Fix:
- Created newsengine/utils.py with run_background_task()
- Replaced ALL 15 raw threading.Thread calls with run_background_task()
- Exception: textextractor/apps.py startup recovery (different pattern, runs once)
- IMPORTANT: Helper lives in newsengine (engine infrastructure), NOT core (display only)

Files changed:
- NEW: newsengine/utils.py
- post/views_api.py — 10 threading calls replaced, dead function removed
- debate/views_api.py — 5 threading calls replaced

================================================================================
ISSUE 2: Silent Exception Swallowing (23 instances across 13 files)
================================================================================

Problem: except Exception: pass / except Exception: return None — with ZERO logging.
         Violates feedback_no_silent_errors rule.

Fix: ALL except blocks now log via logger.error/warning/exception.
     Added import logging + logger = logging.getLogger(__name__) to 8 files.

Files fixed (15 except:pass + 8 except:return):
- newsengine/views_api.py (3 — OG parser, URL validation, URL fetch)
- newsengine/feed_builder.py (1 — feed preference lookup)
- debate/views_api.py (3 — OG parser, URL validation, URL fetch)
- post/views.py (6 — poll enrichment, poem/art/story/travel/newsengine bookmarks)
- newshub/views.py (1 — contributor person lookup)
- newshub/helpers.py (1 — district lookup for slug)
- tools/views.py (1 — spell check API)
- user_account/context_processors.py (1 — avatar URL fetch)
- portal/views.py (1 — moderation queue fetch)
- textextractor/apps.py (1 — startup recovery)
- textextractor/engines/easyocr (1 — image deskew)
- textextractor/engines/whisper (1 — audio extraction)
- bangladesh/views_api.py (1 — TikTok thumbnail fetch)
- core/utils.py (1 — get_user_profile_id split into DoesNotExist + Exception)

================================================================================
ISSUE 3: Duplicate Code in actions-bar.js
================================================================================

Problem: Follow toggle logic copy-pasted 3 times (optimistic, revert on failure,
         revert on error).

Fix: Extracted setFollowButtonsState(allFollowButtons, isFollowing) helper.
     3 blocks replaced with single function call each.

================================================================================
ISSUE 4: Duplicate DOMContentLoaded in core/main.js
================================================================================

Problem: Two separate DOMContentLoaded listeners for active menu.
         First block (lines 58-68) did simple click-based active toggle.
         Second block (lines 71+) did the same PLUS localStorage persistence.
         First block was completely redundant.

Fix: Removed the first DOMContentLoaded block.

================================================================================
ISSUE 5: Data-Driven Refactor — _enrich_related_content_item
================================================================================

Problem: 5 copy-pasted if/elif branches in core/utils.py, each with same
         cursor.execute → row fetch → build dict pattern.

Fix: _RELATED_CONTENT_ENRICHMENT_MAP dict — one entry per content type.
     Single _enrich_related_content_item() function reads config and executes.

================================================================================
VERIFICATION (zero remaining violations)
================================================================================

Post-fix scan results:
- except Exception: pass — 0 remaining (was 15)
- except Exception: return without logging — 0 remaining (was 8)
- Raw threading.Thread outside newsengine — 0 remaining (was 15)
- alert()/confirm() — 0 violations
- .upper()/.toUpperCase() — 0 violations
- SQL injection — 0 vulnerabilities (all parameterized)
- Dead code — 0 remaining
- Newsengine wiring — all 10 imported functions verified (exist, correct signatures, real logic)

================================================================================
CACHE BUMP
================================================================================

Service worker: v452 -> v454
collectstatic: main.js copied to project_static_collected
