APP TROUBLESHOOTING — Known Issues & Ignored Problems Log
==========================================================

This file tracks issues that were observed but NOT fixed at the time.
Every session, check this list. Fix what you can. Don't let things pile up.

RULE: If you see an error/warning/404 in the console or server logs,
either fix it NOW or log it here with date + exact error. Never dismiss
as "pre-existing" or "not from my code" without telling the user.


## ACTIVE ISSUES (unfixed)
--------------------------------------------------------------

(none — all cleared as of 2026-04-14)


## RESOLVED ISSUES
--------------------------------------------------------------

### R-bookwriter-autosave-pyodbc-ntext. Doubled chapter content lost on revisit
Date: 2026-04-22
Was: User typed a chapter, then doubled it by paste. On chapter re-open
     the doubled content was gone — only the original survived. Server
     traceback showed `Cannot convert to text/ntext or collate to
     'Latin1_General_100_CI_AS_SC_UTF8'` on
     api_bookwriter_chapter_autosave's chapter.save(update_fields=[
     'chapter_text_html', 'chapter_text_plain', ...]) call. Same
     pyodbc bug as memory/troubleshooting.md §5b — pyodbc auto-promoted
     the long string to SQL_WLONGVARCHAR (ntext on the wire), which the
     UTF-8 collation rejects at the parameter-binding layer. CAST in
     the query can't save it. JS .catch() swallowed the 500 silently
     (saveChip showed "offline · will retry" briefly), DB never
     received the new content, revisit pulled the OLD content.
Fix: Replaced the .save(update_fields=[...]) with a raw pyodbc cursor
     in views_api_chapter.py:api_bookwriter_chapter_autosave. Pattern:
       import pyodbc
       django_db_connection.ensure_connection()
       raw_cursor = django_db_connection.connection.cursor()
       raw_cursor.setinputsizes([
           (pyodbc.SQL_WVARCHAR, 0, 0),  # chapter_text_html
           (pyodbc.SQL_WVARCHAR, 0, 0),  # chapter_text_plain
           None, None, None, None,       # int + datetimes + WHERE pk
       ])
       raw_cursor.execute("UPDATE [bookwriter].[chapter] SET ... WHERE bookwriter_chapter_id = ?", [...])
       django_db_connection.connection.commit()
     Mirrored values to the in-memory chapter instance so callers below
     see consistent state. Verified with a 9107-char throwaway-chapter
     happy-path test — UPDATE returned 200 and the row carried 9107
     chars in chapter_text_html post-roundtrip.
Why .catch() didn't surface this: the autosave is fire-and-forget for
     UX reasons (don't block typing on a flaky network). Future hardening:
     escalate persistent autosave failures into a visible "save failed —
     copy your text now" inline banner instead of a transient saveChip.

### R-bookwriter-prose-image-feature. Drag-drop inline images
Date: 2026-04-22
Was: User wanted to drop an image into the manuscript prose, drag it to
     reposition, resize without distortion, text wrapping around it.
     None of these existed.
Fix: Built as a self-contained module + matching backend. Six pieces:
     1. core/utils.py:sanitize_user_html now accepts additive
        extra_allowed_tags / extra_allowed_attrs_by_tag /
        extra_allowed_void_tags / attr_value_validators_by_tag_attr.
        Default behaviour unchanged for every other caller.
     2. bookwriter/views_api_helpers.py:sanitize_chapter_prose_html
        — opts in <img> with src/alt/class/style/data-float, each
        constrained by per-attr validators (src must start with
        /media/upload/bookwriter/chapter/, style must match
        `width: Npx`, class restricted to .bookwriter-prose-image*
        family, data-float in {left,right,center,none}).
     3. bookwriter/image_uploads.py — Pillow.verify() validates real
        image bytes (rejects spoofed extensions + image bombs); SVG
        explicitly rejected (XSS via <foreignObject>); 5 MB cap;
        UUID filename to prevent collisions and path traversal.
     4. POST /bookwriter/api/chapter/<id>/image/upload/ — owner-checked,
        returns the public /media URL.
     5. modules/bookwriter-prose-image.js — drag-drop on prose,
        caret-positioning at drop point via document.caretPositionFromPoint
        (with caretRangeFromPoint fallback for Safari), click-cycle
        float (left → center → right → none → left ...), pointerdown
        hit-tested to bottom-right 18×18px corner for resize-with-aspect-
        preserved, immediate autosave on insert (so drop never gets
        stranded if the user closes the tab).
     6. CSS: .bookwriter-prose-image* with native shape-outside:
        margin-box for text wrap on float-left/right.
     Security verified end-to-end via Django shell:
        - upload: 200, real PNG bytes accepted, /media URL returned
        - autosave roundtrip preserves <img src + alt + class + style + data-float>
        - onerror attribute STRIPPED
        - src=javascript:alert(1) DROPPED
        - src=https://evil.example/x.png (cross-origin) DROPPED

### R-bookwriter-layout-page-grows. Page-grows-with-text instead of internal manuscript scroll
Date: 2026-04-22
Was: Earlier the manuscript was a fixed-height internal scroll container
     (`flex: 1; overflow-y: auto` with the documented `min-height: 0` flex
     fix). User feedback: "the border should be responsive and adjustive
     to the text. after its initial height, it should expand with the
     text increasing." The fixed-height + internal scroll model
     contradicted that — the red focus border looked like a small box
     even on long chapters, and contenteditable caret-tracking broke
     when the manuscript clipped content.
Fix: Pivoted to page-grows-with-text:
       - .bookwriter-stage: `overflow: hidden` removed
       - .bookwriter-manuscript: `flex: 1` only; no overflow-y: auto
       - .bookwriter-embedded-wrapper: `height` → `min-height` with
         dvh + JS-measured `--bookwriter-header-offset` so the chrome
         offset is exact (was hardcoded 64px; the real header is taller
         once auth controls + notification bell render).
       - .bookwriter-page is now a flex column
       - .bookwriter-prose is `flex: 1` so the red focus border fills
         the manuscript visible area on a short chapter
     Sticky rails: .bookwriter-rail + .bookwriter-desk now
     `position: sticky; top: var(--bookwriter-header-offset);
     align-self: start; max-height: calc(100dvh - header)` so the
     side panels stay pinned in the viewport as the page (window)
     scrolls past them. Matches Notion / Docs / Reedsy UX.

### R-bookwriter-styling-audit. Styling + best-practices pass
Date: 2026-04-22
Findings + applied fixes:
  - A11Y: 3 form controls had only placeholder, no <label>. Added
    .bookwriter-visually-hidden utility (clip-rect screen-reader-only
    pattern) and wrapped reader comment textarea, beta comment kind
    select, beta comment textarea with hidden labels. Screen readers
    now announce purpose; placeholder no longer relied on as label.
  - Beta accent color: #c46a5d / rgba(196,106,93,*) hardcoded in
    24 sites across page-public-reader.css. Tokenized to
    var(--bookwriter-beta-accent) (solid) and rgba(var(--bookwriter-
    beta-accent-rgb), <alpha>) (transparency variants). Single source
    of truth — change the rgb triplet in the :where() block to
    retheme beta UI globally.
  - Animation timing mismatch: page-public-reader.js setTimeout was
    1600ms but the matching @keyframes ran 1.4s — class lingered
    200ms after the animation settled. Aligned to 1400ms with a
    comment explaining the link to the CSS keyframes duration.
  - Vendor prefix cleanup: -webkit-appearance / -moz-appearance
    redundant on `appearance: none` since 2018. Stripped from
    .bookwriter-chapter-visibility-select (and 3 sibling selects),
    .bookwriter-field input[type="range"], and
    .bookwriter-beta-comment-kind-select. Slider-thumb pseudo-element
    prefixes kept (cross-browser slider styling is fragile).
Deferred (multi-hour cosmetic, low-risk-but-large):
  - Hardcoded font-size in px across ~100 selectors. Should migrate
    to var(--text-xs..5xl) tokens. Defer to focused session — risk
    of unintended visual shifts if any token mapping is wrong.
  - Hardcoded snapshot/cover/history mini colors (~13 sites) using
    raw hex (#f4ede0, #1a1612, etc.) — should map to var(--paper)
    / var(--ink) / var(--accent). Same defer reason.

### R-bookwriter-bug-scan. Bookwriter exhaustive bug scan — 0 violations
Date: 2026-04-22
Scope: amolnama_news/site_apps/bookwriter/ (all CSS / JS / templates / Python).
Categories scanned (per CLAUDE.md):
  - alert() / confirm() / prompt() — 0
  - console.log/debug/info/warn (only console.error in .catch allowed) — 0
  - fetch() without .catch() — 0 (all go through window.bookwriter.apiPost
    / apiGet / apiDelete which terminate every chain)
  - silent empty catch blocks — 0 (all catches log or update saveChip)
  - .toUpperCase() / .upper() — 0
  - inline style with static design rules — 0 (only CSS-custom-property
    hex injection per Gate 4 #2)
  - internal <style> blocks in templates — 0
  - nested <main> tags — 0 (templates extend core/base.html and use
    <section>/<aside>/<div> inside {% block content %})
  - duplicate event listeners — 0
  - null-check gaps on querySelector/getElementById — 0 (every use
    guarded with `if (...)` or `?.`)
  - raw SQL with f-string column interpolation — 0
  - user HTML rendered without sanitize_user_html() — 0
  - !important on display — 0 (only used inside prefers-reduced-motion
    blocks, which is the documented exception)
  - missing focus-visible on interactive elements — 0
  - touch-target <44px on interactive elements — 0
  - multi-line {# #} template comments — 0
  - form elements missing id or name — 0
  - TODO/FIXME/HACK leftovers — 0
  - dead branches (`if False`, code after return) — 0
Result: bookwriter passes the project's hard rules cleanly. No fix
patches generated by this scan. If a future bug emerges in any of these
categories, it'll be from new code added AFTER 2026-04-22.

### R-bookwriter-naming-audit. Bookwriter naming-convention pass (CSS classes + JS variables)
Date: 2026-04-22
Was: Pre-existing short / abbreviated names littered the bookwriter app —
     `.bookwriter-ch-num`, `.bookwriter-ch-title`, `.bookwriter-cv-frame`,
     `.bookwriter-bg-opt`, `.bookwriter-b-name`, `.bookwriter-tool-btn.bookwriter-b`,
     plus 18+ single-letter forEach callback parameters (`x`, `m`, `t`, `b`,
     `p`, `h`, `c`, `e`, `ch`). Linter (`_check_naming.py`) passed because
     it only enforces the `bookwriter-` prefix, not descriptive length.
Fix: Renamed across CSS, JS, templates in lockstep:
     - `.bookwriter-ch-*` → `.bookwriter-chapter-rail-row-*`
       (avoids collision with `.bookwriter-chapter-title` editor h1)
     - `.bookwriter-tool-btn.bookwriter-{b,i,u}` → `.bookwriter-tool-btn.bookwriter-tool-{bold,italic,underline}`
     - `.bookwriter-b-{name,role}` → `.bookwriter-bible-character-{name,role}`
     - `.bookwriter-cv-*` → `.bookwriter-cover-art-*`
     - `.bookwriter-bg-opt*` → `.bookwriter-background-option*`
     - `.bookwriter-bg-options` → `.bookwriter-background-options` (cascaded by prefix replace)
     - JS forEach callbacks: `function (x)` → `function (chapterRowElement)`
       etc. (18 occurrences across cover designer, font pick, palette,
       background, history, drag handlers, sprint, share-permission,
       bible item / category)
     - Drag-reorder handler vars: `ch` → `chapterRowElement`,
       `e` → `dragStartEvent` / `dragOverEvent` / `dropEvent`,
       `parentList` → `parentChaptersListElement`,
       `listItems` → `siblingChapterRowElements`,
       `fromIndex` → `draggedFromIndex`,
       `toIndex` → `droppedAtIndex`,
       `draggedEl` → `draggedChapterRowElement`
     - public-reader.js for-loop indices: `i` → `textNodeIndex` /
       `anchorRecordIndex`; `rec` → `textNodeRecord` / `anchorRecord`
Files changed: page-inkwell.css, page-inkwell.js, page-public-reader.js,
     _inkwell_body.html.
Form-element id+name audit: 0 violations (all <input>/<select>/<textarea>/
     <button> already have id+name; hidden inputs exempt).
File-size note: page-inkwell.js sits at 2518 LOC after these renames —
     candidate for extraction into bookwriter-cover-designer.js,
     bookwriter-formatting-toolbar.js, bookwriter-mode-switch.js,
     bookwriter-sprint-timer.js. Deferred — each extraction needs its
     own happy-path verification pass.

### R-scroll. Page jumps to top on nav / form-action / reload — two mechanisms
Date: 2026-04-21 (consolidates R0 from 2026-04-16)

Two distinct ways the scroll resets to top. Diagnose first (DevTools Network):
full document reload = mechanism A; XHR + `<main>` swap = mechanism B.

Mechanism A — full reload (`window.location.search/href = ...`, plain `<a href>`)
  Browser default scroll behaviour on a fresh load is (0, 0) UNLESS the URL has
  a fragment (`#anchor`) pointing at an in-DOM id.
  Fix: add `id="..."` to the in-DOM element the user was focused on, append
  `#same-id` to every URL the JS or template emits. Build URLs explicitly as
  `pathname + '?' + params + '#anchor'` — `window.location.search = ...` DROPS
  any hash. Verified: Quiz Panel review queue (`id="quizadmin-review-card"` +
  `#quizadmin-review-card` on every approve/reject/prev/next URL).

Mechanism B — SPA navigation (sidebar clicks → spa-navigation.js)
  spa-navigation.js Step 8 used to call `window.scrollTo(0, 0)` on every
  forward nav. Wrong for any long-scroll page (Inkwell manuscript, feed,
  bookmarks). Reported 10+ times before being properly committed + documented.
  Fix: per-URL save/restore in spa-navigation.js —
    Step 0 (leaving): `sessionStorage['spa_scroll:' + pathname] = scrollY`
    Step 8 (arriving): if `sessionStorage['spa_scroll:' + destPath]` exists,
                        restore it; else scroll to top.
  Universal across every sidebar link. Sidebar's OWN scrollTop preservation
  (sidebar-navigation.js sessionStorage) was already in place; this is the
  body/window equivalent.

  Sub-bug B2: clicking the SAME active sidebar link twice still scrolled to
  top. Click handler early-returned on `url === pathname` without calling
  `event.preventDefault()`, so the browser's default same-href reload kicked
  in → fresh page load → scroll (0, 0). Fix: preventDefault on the same-
  pathname branch too. True no-op now.

Files: spa-navigation.js (Step 0 + Step 8), seo/views.py (cache bump).

Verification (browser, do every time):
  1. Scroll mid-page on home feed.
  2. Click কলম sidebar → /bookwriter/ at top (first visit).
  3. Scroll mid-Inkwell.
  4. Click any other sidebar item → leaves bookwriter.
  5. Click কলম again → MUST restore step 3 scroll position.

Why not Turbo: project already has working custom PJAX SPA. Swapping engines
would force re-validating cleanup registry / Tom Select / Quill destroy paths
across every page. ~10-line save/restore inside the existing SPA gives the
same UX without the rip-out.

Rule: if not in this file on the same commit, it's not fixed.

### R-bookwriter-inline-style-pattern. Inline style="background:linear-gradient(...)" violated Gate 4 #2 even for DB-driven values
Date: 2026-04-20
Was: Bible avatars had `style="background:linear-gradient(135deg, {{ hex_1 }}, {{ hex_2 }});"`.
     Both the gradient angle (135deg) and the design (linear-gradient + fallback)
     lived inline. The naive read of "dynamic values are exempt" allowed the
     whole inline block, mixing design and data.
Root cause: We treated the entire gradient as one indivisible "dynamic value".
     In reality, only the two hex values are dynamic. The angle, fallback
     colours, and gradient syntax are static design.
Fix: Adopted the CSS-custom-property injection pattern:
       CSS:    .bookwriter-bible-avatar {
                  --bookwriter-bible-avatar-hex-1: var(--ochre);    /* fallback */
                  --bookwriter-bible-avatar-hex-2: var(--accent);   /* fallback */
                  background: linear-gradient(135deg,
                    var(--bookwriter-bible-avatar-hex-1),
                    var(--bookwriter-bible-avatar-hex-2));
                }
       Template: style="--bookwriter-bible-avatar-hex-1: {{ hex_1 }};
                        --bookwriter-bible-avatar-hex-2: {{ hex_2 }};"
       JS:      el.style.setProperty('--bookwriter-bible-avatar-hex-1', hex1);
                el.style.setProperty('--bookwriter-bible-avatar-hex-2', hex2);
     Inline still exists but carries data only (no design, no syntax).
     Now codified in CLAUDE.md Gate 4 #2.
Lesson: When you find yourself emitting inline `style="..."`, ask "is the
     ENTIRE inline value dynamic, or only N specific data points?" If the
     latter, use custom properties + a CSS rule that composes them. The
     inline attribute should never carry a CSS function call (linear-gradient,
     calc, rgb, etc.) — those are design.

### R-bookwriter-classes. Bulk CSS class rename broke styling on cached browsers
Date: 2026-04-20
Was: After a bulk rename of every bookwriter CSS class to add the
     `bookwriter-` prefix, the inkwell page lost its drop-cap (first
     letter big + accent color) and other visual cues. The CSS file
     was correct, the templates were correct, but the SERVICE WORKER
     was still serving the previous CSS where the selector was `.prose`,
     not `.bookwriter-prose`.
Root cause: Two-fold. (a) The bulk-rename script only rewrote class
     names in CSS rules, in HTML `class="..."` attributes, and in JS
     `classList.*` / `.className` / dot-prefixed selector strings.
     It MISSED bare class-name strings inside JS string concatenations
     (e.g. `' is-resolved'` in a ternary), inside JS `innerHTML` template
     strings (`<div class="margin-notes-empty">`), and inside object-
     literal options whose key implied a class (e.g.
     `confirmingClass: 'confirming'`). (b) Service worker cache was
     not bumped after the CSS rename.
Fix: Wrote a context-aware pass-2 script that handles innerHTML class=
     attrs, classList.* calls including leading-whitespace forms,
     `.className =/+=` full RHS (including ternaries), and object-literal
     properties whose key ends in `Class`. Then bumped CACHE_NAME from
     v759 → v760 + ran collectstatic. User had to hard-reload once
     (Ctrl+Shift+R) for the service worker to refresh.
Lesson: When doing a bulk CSS class rename, the JS rewrite MUST cover:
     (1) classList.add/remove/contains/toggle/replace string literals,
     (2) `.className = / +=` assignments INCLUDING the entire RHS
     expression (ternaries, concats),
     (3) innerHTML / template string `class="..."` attributes,
     (4) object-literal property values whose KEY name ends in `Class`,
     (5) dot-prefixed selector strings inside querySelector etc.
     Bumping CACHE_NAME + collectstatic is non-negotiable after any
     CSS edit, even pure renames.



### R1. CSRF token wrong pattern in call-webrtc.js
Date: 2026-04-14
Was: Used meta[name="csrf-token"] — doesn't exist in this project.
Fix: Changed to getCsrfTokenValue() global function from csrf-token.js.

### R2. @latest in Twemoji CDN URL detected as mention
Date: 2026-04-14
Was: URL contained @latest which mention regex @([\w.-]+) matched.
Fix: Changed to pinned version @14.0.2 in both text_highlight.py and
post-home.js getFlagImageUrl().

### R3. @14.0.2 still detected as mention (server-side)
Date: 2026-04-14
Was: text_highlight.py mention regex matched @14 in the Twemoji URL.
Fix: Moved flag replacement to run AFTER HTML build in
highlight_entities_in_text(), so mention detection runs on raw text
(which has Unicode flag chars, not URLs).

### R4. @14.0.2 STILL corrupted (client-side duplicate linkifier)
Date: 2026-04-14
Was: post-feed-interactions.js line 1383 had a JS mention regex that ran
on innerHTML of .post-card-text elements. It matched @14 inside <img>
src attributes and replaced it with an <a> tag, breaking the Twemoji URL.
Root cause: DUPLICATE mention linkifier — server-side text_highlight.py
already handles @mention → <a> conversion. The JS one was redundant.
Fix: Removed the JS mention linkifier entirely from
post-feed-interactions.js. Server-side is the single source of truth.
Lesson: Duplicate logic is a bug factory. One source of truth always.

### R5. Early return skipping flag replacement
Date: 2026-04-14
Was: highlight_entities_in_text() returned escape(text) early when no
entities found, skipping the flag replacement at the bottom.
Fix: Added _replace_flag_emoji_with_twemoji() to the early return path.

### R6. Emoji picker collapsing on click (post composer)
Date: 2026-04-14
Was: style.display toggle conflicted with HTML hidden attribute.
Fix: Switched to .hidden property. Added emoji picker state to
isComposerInDraftMode() to prevent auto-collapse. Emoji picker stays
open while clicking emojis, closes on textarea mousedown.

### R7. Profile card border-radius clipping text
Date: 2026-04-14
Was: .profile-card used --radius-full (9999px pill shape).
Fix: Changed to --radius (12px).

### R8. Post composer border-radius clipping text
Date: 2026-04-14
Was: .post-composer used --radius-full (9999px pill shape).
Fix: Changed to --radius-lg (16px).

### R9. Double border on messenger textarea focus
Date: 2026-04-14
Was: :focus border-color change + :focus-visible outline = double ring.
Fix: Set :focus-visible { outline: none } on textarea.

### R10. Promo sample images 404
Date: 2026-04-14
Was: promo_builders.py TOOLS_CATALOG referenced .webp images that don't
exist (split-pdf.webp, background-remover.webp, zip-creator.webp, etc.)
Fix: Cleared all tool_sample_image_url values to empty string.
Template already degrades gracefully (onerror hides image section).
TODO: Create actual promo images and re-add URLs.

### R11. graph_user_dwell_on_post NULL $to_id INSERT failure
Date: 2026-04-14
Error: Cannot insert NULL into column 'to_id_...' (515)
Was: Dwell edge INSERT subquery (SELECT $node_id FROM graph_post_node
WHERE link_post_id = %s) returned NULL because post node didn't exist.
ensure_user_node_exists() was called but create_post_node() was not.
Fix: Added create_post_node() call before dwell edge insert in
interest_graph.py. Looks up actual post author from Post model.
Location: newsengine/interest_graph.py, record_dwell_time() function.

### R12. feed_fanout MATCH + JOIN incompatibility
Date: 2026-04-14
Error: Identifier 'u' in a MATCH clause is used with a JOIN clause (13920)
Was: Query used JOIN [account].[user_profile] with MATCH clause on graph
tables. SQL Server doesn't allow JOIN on graph node tables when MATCH
is used in WHERE.
Fix: Replaced JOIN with EXISTS subquery for active user check.
Location: newsengine/feed_fanout.py, _fanout_to_interested_users_by_topic().

### R13. embeddings store ntext/UTF-8 collation failure
Date: 2026-04-14
Error: Cannot convert to text/ntext or collate to
'Latin1_General_100_CI_AS_SC_UTF8' (4189)
Was: MERGE statement with CAST(%s AS NVARCHAR(MAX)) still failed because
pyodbc binds the parameter as SQL_WLONGVARCHAR (ntext) before SQL Server
can cast it. The MERGE parameterization is more complex for pyodbc.
Fix: Replaced MERGE with two-step UPDATE then INSERT (if rowcount=0).
Simpler parameterization avoids pyodbc ntext binding.
Location: newsengine/embeddings.py, store_embedding() function.

### R14. Duplicate server log lines in dev
Date: 2026-04-14
Was: Every HTTP request logged twice in console.
Fix: Added propagate: False to django.server and django.request loggers
in dev.py LOGGING config.
Location: settings/dev.py.

### R15. Flag emojis not rendering on Windows
Date: 2026-04-14
Was: Windows doesn't render flag emojis (shows as "BD" text instead of flag).
Fix: Added _replace_flag_emoji_with_twemoji() in text_highlight.py that
converts regional indicator Unicode pairs to Twemoji CDN SVG <img> tags.
Runs after entity detection + HTML build. CSS class: .post-twemoji-flag.
Also added flag rendering in emoji picker (getFlagImageUrl + country code
labels) using Twemoji CDN images.
Location: post/text_highlight.py, post/static/post/assets/css/pages/post-home.css.

### R16. JSON island in HTML can leak </script> from user-controlled fields
Date: 2026-04-17
Was: Mastermind multi-player lobby pages rendered server-side state via
<script type="application/json">{{ payload_json|safe }}</script>. The payload
contains player.display_name from UserProfile. A malicious display_name
containing </script> would break out of the JSON block.
Fix: Switched to Django's built-in {{ payload|json_script:"id" }} filter
which auto-escapes </ → \u003c. View must pre-stringify any datetime /
Decimal values because json_script can't serialise them — added
mastermind/views._stringify_dates() helper that walks the dict/list and
ISO-formats datetimes, floats Decimals.
Location: mastermind/views.py, mastermind/templates/mastermind/pages/lobby_player.html,
quizadmin/templates/quizadmin/pages/quiz_host.html, quizadmin/views.py.

### R17. Django ORM __contains substring match has prefix-collision bug
Date: 2026-04-17
Was: lobby.submit_lobby_answer used __contains=f'"question_index": {N}' to
detect duplicate submissions. The substring "question_index": 1 also matches
"question_index": 10 / 11 / 12 / etc., so a player who answered question 1
would be falsely blocked from answering questions 10+.
Fix: Anchor the match with a trailing comma — f'"question_index": {N},'.
Works because answer_submitted event payloads always have question_index
followed by other fields (is_correct, points_earned), so the comma is
guaranteed to be present in the JSON output.
Location: mastermind/lobby.py, submit_lobby_answer().

### R18. .toUpperCase() on identifier input is OK; on display data is not
Date: 2026-04-17
Was: Bug-scan flagged .upper() / .toUpperCase() in lobby.py + views.py for
join codes. CLAUDE.md rule says no case conversion anywhere.
Decision: KEPT for join codes — they are identifiers from a deliberately
uppercase-only alphabet (BCDFGHJKLMNPQRSTVWXYZ23456789), not user-facing
display data. Normalising user input to match the stored case is required.
The rule applies to status_code / display data, not fixed-alphabet identifiers.
Location: mastermind/lobby.py:_generate_unique_join_code, get_lobby_state.

### R19. Django Channels consumer + sync engine functions
Date: 2026-04-17
Was: WebSocket consumer (LobbyConsumer) needs to call sync engine functions
(create_lobby, get_lobby_state, etc.) that hit the SQL Server ORM.
Pattern: Wrap every engine call in @database_sync_to_async via a helper
method like `async def _engine_call(self, fn, *args, **kwargs)`. Don't call
sync ORM directly from async receive() — Django will raise
SynchronousOnlyOperation.
Location: mastermind/consumers_lobby.py.

### R20. Mobile-friendly tables — data-label reflow pattern
Date: 2026-04-17
Was: Quiz Panel staff inbox tables (flagged questions, leaderboards) horizontal-
scroll on phones. Cards are easier to triage than scrolling.
Fix pattern: Add data-label="Column name" to every <td>. CSS at ≤720px sets
table/tbody/tr/td to display:block, hides thead, and adds td::before {
content: attr(data-label) } so each cell becomes a labelled stack.
Location: quizadmin/templates/quizadmin/pages/flagged_questions.html,
quizadmin/static/quizadmin/assets/css/pages/page-flagged-questions.css.

### R21. Web Audio API — non-essential sound that must never break the app
Date: 2026-04-17
Was: Lobby pages need sound effects (correct, wrong, time-warning, etc.)
without external audio files (CSP-safe + free). Sound is non-essential —
gameplay must continue if the audio context fails.
Pattern (mastermind/static/mastermind/assets/js/lobby-sfx.js):
  - AudioContext created lazily on first play() call (browser autoplay policy
    requires user gesture — don't construct on script load)
  - Each sound is composed at play time as a tiny synth pattern
    (oscillator + gainNode with smooth attack/release envelopes — no clicks)
  - Three intentional silent catches: localStorage blocked (private mode),
    AudioContext.resume() failed (autoplay policy), _playToneSequence threw
    (browser quirk). All annotated. Sound is explicitly non-essential so
    swallow + comment is the correct design (NOT a bug).
  - Per-event 150ms debounce so spammed state messages don't blast the user.
  - Honours prefers-reduced-motion AND localStorage("mastermind_lobby_sfx")
    so users can mute persistently.
  - Module exposes wireToggleButton(buttonElement) — single source of truth
    for wiring 🔊 / 🔇 toggle buttons (used by both lobby-player.js and
    lobby-host.js with one line each, no duplication).
Location: mastermind/static/mastermind/assets/js/lobby-sfx.js.

### R24. Long Bengali text + raw pyodbc INSERT pattern (chunk_text fix)
Date: 2026-04-17
Was: Saving chapter text via the new book editor needed to insert long
Bengali strings into coll_book_chunk.chunk_text (NVARCHAR(MAX), UTF-8
collation). ODBC Driver 17 promotes long parameters to SQL_WLONGVARCHAR
(ntext), which fails the _SC collation cast.
Fix: Reuse the proven pattern from ai_generator.ingest_book_from_pdf —
drop to the raw pyodbc cursor on the Django connection, set
cursor.setinputsizes() with (pyodbc.SQL_WVARCHAR, 0, 0) on the chunk_text
parameter only, INSERT, commit. See site_apps/mastermind/book_editor.py
function _raw_insert_book_chunk(). Same fix as the original PDF chunker
— do NOT extract it to a "shared utility" because the column index in
setinputsizes is positional and fragile. Better to copy + comment.
Location: site_apps/mastermind/book_editor.py.

### R25. Django |linebreaks vs custom paragraph splitting in templates
Date: 2026-04-17
Was: Initial book_reader.html template used a non-existent
|linebreaks_to_paragraphs filter to wrap chunks in <p> tags. Django
doesn't have that filter — built-in is just |linebreaks (wraps blank-
line-separated chunks in <p> AND auto-escapes content).
Fix: Use Django's built-in {{ first_chapter_text|linebreaks }} for the
SSR first-paint chapter; use JS String.split + escape + manual <p>
wrapping for client-side chapter switches. Both paths produce identical
DOM output and both auto-escape user content (XSS-safe).
Location: mastermind/templates/mastermind/pages/book_reader.html,
mastermind/static/mastermind/assets/js/book-reader.js.

### R26. Public chapter-text endpoint — separate from edit endpoint
Date: 2026-04-17
Was: The reader page needed to lazy-load chapters as the visitor clicks
TOC entries. Reusing the editor endpoint /api/.../get-text/ would have
forced @login_required + owner/staff permission — anonymous public
readers couldn't switch chapters.
Fix: Added a separate /api/book/<id>/chapter/<id>/public-text/ endpoint
with NO @login_required and a strict filter on book_status_code='published'.
Drafts / reviews / archives 404. Both endpoints share the same
get_chapter_with_text() engine function — single source of truth.
Pattern: when an authenticated endpoint needs an anonymous-readable
companion, add a public sibling rather than try to make one endpoint
handle both auth modes. Cleaner permission story.
Location: site_apps/mastermind/views_api.py (api_book_chapter_get_text +
api_book_chapter_get_text_public).

### R27. Imported book has chunks but no chapters → "No chapters yet"
Date: 2026-04-17
Was: ai_generator.ingest_book_from_pdf creates rows in coll_book_chunk
but does NOT auto-create rows in coll_book_chapter. The book editor's
chapter sidebar reads from coll_book_chapter — so for a book imported
via PDF (where the user never manually added chapters), the editor
showed "No chapters yet" even though 66 pages of text existed in chunks.
Fix: book_editor.ensure_default_chapter_for_orphan_chunks(book_id) —
idempotent migration that creates ONE default chapter and bulk-updates
every orphan chunk to point to it. Called from book_editor_page on every
visit (no-op when chapters exist). Result: legacy imported books open in
the editor with all their text immediately accessible.
Location: site_apps/mastermind/book_editor.py
ensure_default_chapter_for_orphan_chunks() + quizadmin/views.py
book_editor_page() entry block.

### R28. Hardcoded is_staff_user=False breaks staff edits on legacy data
Date: 2026-04-17
Was: book_editor.add_chapter_to_book had `is_staff_user=False` baked
into the call to _user_can_edit_book. For a legacy book where
link_created_by_user_profile_id is NULL (pre-dates the column), the
permission check returned False even when the calling user was staff.
Net effect: 400 Bad Request on every "+ Add chapter" click.
Fix: Add is_staff_user parameter to add_chapter_to_book; the API
endpoint extracts it from request.user.is_staff and passes through.
Same pattern applied to update_chapter_metadata and the new
update_book_metadata / delete_chapter / reorder_chapter functions.
Lesson: never bake permission flags into helper signatures — always
accept them from the caller so the request context stays the source of
truth. A staff bypass is fine, but it has to ACTUALLY honour staff.
Location: site_apps/mastermind/book_editor.py + views_api.py.

### R29. CSS custom-property theming via [data-reader-theme="..."]
Date: 2026-04-17
Was: Book reader v2 needed light / sepia / dark themes that switch
instantly without page reload AND cascade onto every element inside the
reader (background, text, borders, accent, muted text). Class swapping
(adding/removing .theme-dark) requires every styled element to know about
the theme class — fragile.
Pattern: Set CSS custom properties on the root reader element gated by
[data-reader-theme="sepia"] and [data-reader-theme="dark"] attribute
selectors. The base rule defines all variables (--reader-bg, --reader-text,
--reader-muted, --reader-border, --reader-pane-bg, --reader-accent).
Theme overrides re-set ONLY those variables. Every child uses
var(--reader-*), so swapping the data attribute on the root re-cascades
the entire palette.
Bonus: same pattern works for font-size — [data-reader-font-size="..."]
gates a single rule that sets font-size on .reader-pane.
Location: mastermind/static/mastermind/assets/css/components/book-reader.css.

### R30. Engine functions accept is_staff_user explicitly (no implicit context)
Date: 2026-04-17
Was: The pure-engine modules (mastermind/book_editor.py) used to default
is_staff_user=False so a forgetful caller would get the safer behaviour.
Bit me badly when add_chapter_to_book was called from an API view that
didn't pass it through — see R28.
Pattern: For every engine helper that gates on is_staff_user, the
parameter MUST be supplied explicitly by the caller. The default value
remains False for safety in shell tests, but every API endpoint is
responsible for passing `bool(request.user.is_staff or
request.user.is_superuser)`. Document in the function docstring.
Location: site_apps/mastermind/book_editor.py.

### R23. Modularising shared lobby JS — host + player + shared module pattern
Date: 2026-04-17
Was: lobby-host.js and lobby-player.js were each defining identical
  _escapeHtml, _setError, _showState, _connect, _send, _playSfx,
  _renderRoster, _renderLeaderboard, _renderFinalLeaderboard, plus an inline
  progress-bar update — about 170 lines of cross-file duplication.
Fix: Created mastermind/static/mastermind/assets/js/lobby-shared.js exposing
  window.mastermindLobbyShared.{escapeHtml, setError, showState,
  connectWebSocket, sendWebSocketMessage, playSfx, renderLeaderboard,
  renderFinalLeaderboard, renderRoster, updateProgressBar}.
  Both consumers now keep ~1-line wrappers that delegate to the shared module
  (so the local API surface stays consistent), but the implementation is one
  source of truth. Loaded BEFORE the consumer scripts via
  <script src="lobby-shared.js" defer>.
Known remaining duplication (out of scope for this pass): the question-type
  renderers (_renderRadioGroup, _renderCheckboxGroup, _renderFillBlank,
  _renderTextarea, _renderOrdering, _renderMatching) are duplicated between
  lobby-player.js (added today) and quiz-take.js (pre-existing, 4 production
  consumers). Extracting these would require restructuring how closure state
  is captured — deferred to a separate refactor session.
Location: mastermind/static/mastermind/assets/js/lobby-shared.js,
          mastermind/static/mastermind/assets/js/lobby-host.js,
          mastermind/static/mastermind/assets/js/lobby-player.js.

### R22. Floating action buttons need fixed positioning on mobile
Date: 2026-04-17
Was: SFX-toggle button on the host lobby page was position:absolute at top-right
of .mastermind-lobby. On desktop it sat above the share-code panel cleanly.
On mobile (≤768px) the lobby collapses to single column and the share-code
panel becomes the first child — the absolute toggle overlapped it.
Fix: At ≤768px switch to position:fixed; bottom: var(--space-4); right: var(--space-3);
so the toggle anchors to the viewport corner instead of the (now-collapsing)
container. Adds elevation-3 shadow to make it readable above scrolling content.
Location: mastermind/static/mastermind/assets/css/components/lobby.css,
.mastermind-lobby-sfx-toggle-floating.



### B1. Bookwriter — request.session not available under RequestFactory
File: bookwriter/views_api.py:api_bookwriter_release_view_record
Symptom: Test fails with "WSGIRequest object has no attribute session".
Cause: SessionMiddleware always runs in production but RequestFactory skips it.
Fix: Use getattr(request, 'session', None) when accessing session_key, so the
test path doesn't blow up. Production unchanged because the middleware always sets it.

### B2. Bookwriter — XSS via hex-color interpolation into style=""
File: bookwriter/views_api.py:_is_valid_hex_color
Symptom: Saving entry_avatar_color_hex='red";attack();"' to a bible entry got
echoed unescaped into a style="background:linear-gradient(135deg,...)" attribute.
Cause: The save endpoint only stripped HTML tags via _strip_html_to_plain — it
preserved quotes/semicolons.
Fix: Module-level HEX_COLOR_PATTERN = re.compile(r'^#[0-9a-fA-F]{3,8}$') with a
shared _is_valid_hex_color() helper. Used by both bible avatar fields and
cover_palette_*_hex_override fields. Bad payloads return 400.

### B3. Bookwriter — view tracking codes must match seeded ref tables
File: bookwriter/static/bookwriter/assets/js/pages/page-public-reader.js
Symptom: First impl sent device_code='phone' and referrer_code='external'.
Both got 400 because seeded values are mobile/tablet/desktop/unknown for
device and direct/search/social/email/rss/internal/unknown for referrer.
Fix: Always cross-check JS lookup tables against the seeded ref rows before
shipping. Bookwriter's seeded codes are documented in app-bookwriter.txt
under TABLE INVENTORY.

### B4. Bookwriter — bible attribute editor needs hidden JSON field
File: bookwriter/templates/.../_inkwell_body.html (bible-detail)
Symptom: Bible entry attributes stored as JSON. UI was a row of key/value
inputs, but state needed to round-trip across detail-pane swaps (clicking
another entry re-renders the pane via innerHTML).
Fix: Hidden <input> #bookwriter-bible-entry-attributes-json carries the
serialized JSON value. JS hydrates the visible rows from the hidden field
on render and serializes back on every input event.

### B5. Bookwriter — chapter rail status picker MUST be hover-revealed
File: bookwriter/static/bookwriter/assets/css/pages/page-inkwell.css
(.bookwriter-chapter-status-picker-row)
Symptom: Showing two <select> elements per chapter row by default cluttered
the rail and broke the calligraphic/minimalist aesthetic.
Fix: display: none baseline + .chapter:hover .bookwriter-chapter-status-picker-row {
display: flex; }. focus-within also reveals so keyboard users can reach the
selects via tab.

### B6. Bookwriter — auto-create BetaReader on first beta link visit
File: bookwriter/views.py:_resolve_beta_reader_for_share_token
Pattern: A logged-in viewer arriving at /beta/<token>/ for the first time
gets a BetaReader row auto-created so subsequent comment posts have a real
reader_id to attach to. Idempotent — subsequent visits return the existing
row. Uses share_link.beta_permission_code as the new reader's permission.

### B7. Bookwriter — _strip_html_to_plain leaves quotes/semicolons untouched
General pattern: bookwriter's _strip_html_to_plain regex is "<[^>]+>" — it
only strips HTML tags. For free-form text fields (entry_role, entry_name,
margin notes, comment_text) this is fine because the resulting string is
rendered via Django's autoescape (not |safe). NEVER use _strip_html_to_plain
output inside a style="" / data-attr / inline-onclick context — those need
either escapejs / escape filters or a strict pattern validator.

### B8. Bookwriter — FK constraint blocks DELETE when test cleanup wrong-orders
File: development-only test scripts
Symptom: Trying to DELETE a SerialRelease while EngagementSerialReaction
rows still reference it raises pyodbc.IntegrityError 547.
Fix: Always delete children FIRST in test cleanup:
  EngagementSerialReaction.objects.all().delete()
  EngagementSerialComment.objects.all().delete()
  EngagementSerialView.objects.all().delete()
  EngagementSerialSubscriber.objects.all().delete()
  BetaComment.objects.all().delete()
  SerialRelease.objects.filter(...).delete()

### B9. Bookwriter — public_chapter_slug collision suffixing
File: bookwriter/views_api.py:api_bookwriter_chapter_publish
Pattern: serial_release.public_chapter_slug has a UNIQUE constraint. If two
chapters in DIFFERENT books happen to slugify to the same string, the second
publish would fail. Fix: detect collision before insert and append "-<chapter_id>"
suffix. Same suffix pattern is reused on title-rename republish.
