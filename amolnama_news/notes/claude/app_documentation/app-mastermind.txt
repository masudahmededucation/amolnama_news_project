MASTERMIND APP — Architecture & Documentation
==============================================
Last updated: 2026-04-17

PURPOSE
-------
Quiz engine — the brain for all knowledge-testing features.
Ingests books (PDF/OCR), generates questions (manual + AI), runs
randomized quizzes, tracks scores, streaks, mastery, and badges.

NO display logic. NO templates. NO CSS/JS. Backend brain only.
Consumer apps (historybd, constitutionbd, etc.) own their UI.
Staff admin UI lives in quizadmin app (separate).


ARCHITECTURE
------------
Schema: [mastermind].*
27 tables, all unmanaged Django models.

Table naming convention:
  coll_*   = collection (growing data from users/AI/staff)
  ref_*    = reference (static admin-only config, rarely changes)
  eng_*    = engine (computed/derived data)
  map_*    = junction (many-to-many relationships)

All table descriptions stored in DB via SQL Server extended properties.
Query: SELECT t.name, ep.value FROM sys.tables t LEFT JOIN
       sys.extended_properties ep ON ep.major_id = t.object_id
       WHERE t.schema_id = SCHEMA_ID('mastermind');


TABLE DICTIONARY (27 tables)
-----------------------------

Source Material:
  coll_book                              Books/PDFs ingested for question generation
  coll_book_chapter                      Chapters within books (for source citation)
  coll_book_chunk                        OCR text chunks from book pages (~300 words each)

Quiz Definition:
  coll_quiz                              Quiz definitions — title, rules, scoring, rewards
  coll_quiz_topic                        Quiz subject areas (bd_constitution, bcs_exam, etc.)
  coll_quiz_source_registry              Public registry of books (transparency / anti-bias)

Question Bank:
  coll_question                          Universal question bank (AI-generated + manual)
  coll_question_option                   Answer options per question (ক/খ/গ/ঘ)
  coll_question_report                   User-reported problems with questions

Quiz Sessions:
  coll_quiz_session                      One user's attempt at taking a quiz
  coll_quiz_session_question             Per-question response within a session

Gamification:
  coll_user_quiz_badge                   Quiz badges earned by users
  coll_user_card                         Spaced-repetition flashcards (Anki SM-2)
  coll_user_streak                       Daily activity streak records
  coll_streak_freeze                     Streak freeze tokens

AI Pipeline:
  coll_generation_job                    AI question-generation run records
  coll_background_job                    Background task tracking

Reference (static):
  ref_quiz_badge                         Badge definitions (first_quiz, perfect_score, etc.)
  ref_quiz_difficulty_level              easy / medium / hard / expert (4 rows, never changes)
  ref_quiz_question_type                 mcq_single / mcq_multi / true_false / fill_blank / essay
  ref_quiz_question_tag                  Cross-cutting tags for question organization

Engine (computed):
  eng_quiz_semantic_embedding            Semantic vectors for dedup + similarity search
  eng_quiz_question_option_analytics     Per-option selection stats
  eng_user_quiz_topic_mastery            Per-user proficiency per topic

Junctions:
  map_quiz_question_pool                 Which questions belong to which quiz
  map_quiz_question_source               Question → book/chapter source citations
  map_quiz_question_tag                  Question → tag assignments


E2E DATA FLOW
-------------

=== MANUAL QUIZ CREATION (staff via Quiz Panel) ===

1. Staff opens Quiz Panel → /quizadmin/quiz/create/
2. Fills quiz title, topic, rules, rewards → submits
3. quiz_builder.py:create_quiz_with_questions()
   → INSERT coll_quiz (title, topic, pass%, timer, rewards)
   → For each question in the form:
      → INSERT coll_question (text, type, difficulty, source citation)
      → INSERT coll_question_option × N (ক/খ/গ/ঘ with is_correct flag)
      → INSERT map_quiz_question_pool (links question to quiz)
   → All in one transaction.atomic()
4. Quiz appears on /quizadmin/quiz/ list with status=draft
5. Staff changes status to published → quiz is live

=== AI QUIZ GENERATION (PDF → Ollama → NLI gate) ===

1. Staff uploads PDF book
   → INSERT coll_book (title, file_path)
2. Staff triggers ingestion
   → textextractor PyMuPDF + Tesseract OCR → page text
   → Split into ~300-word chunks
   → Raw pyodbc INSERT coll_book_chunk (with setinputsizes to avoid ntext bug)
   → Embed each chunk → INSERT eng_quiz_semantic_embedding
3. Staff triggers generation
   → INSERT coll_generation_job (status=processing)
   → For each chunk:
      → Build prompt with chunk text
      → POST to Ollama localhost:11434 (llama3.2:3b, num_predict=1500)
      → Parse JSON response
      → For each question in response:
         → Tier 0: word-match pre-filter (≥50% words present → pass)
         → Tier 1: bi-encoder similarity (< 0.30 → reject, otherwise → Tier 2)
         → Tier 2: mDeBERTa NLI entailment (entail ≥ 0.50 AND > contra → pass)
         → Dedup check: cosine similarity > 0.90 against existing questions → reject
         → INSERT coll_question (status=review, NLI scores persisted)
         → INSERT coll_question_option
         → INSERT eng_quiz_semantic_embedding (for future dedup)
   → UPDATE coll_generation_job (status=completed, counts)
4. Staff reviews in Quiz Panel → /quizadmin/review/
   → Sees question + source chunk + NLI scores side-by-side
   → Keyboard: A=approve, R=reject, J=next, K=prev
   → Approve → UPDATE coll_question (status=published)
   → Reject → UPDATE coll_question (status=archived, is_active=0)

=== QUIZ TAKING (user via consumer app) ===

1. User starts quiz
   → engine.py:start_exam_session()
   → SELECT questions from map_quiz_question_pool
   → Deduplicate variant groups (question_group_id)
   → Randomize question order (if quiz.exam_shuffle_questions)
   → Randomize option order per question (if quiz.exam_shuffle_options)
   → INSERT coll_quiz_session (status=in_progress)
   → INSERT coll_quiz_session_question × N (display order frozen)
2. User answers each question
   → engine.py:submit_answer()
   → Grade: MCQ checks is_correct, fill_blank does NFC-normalized comparison
   → UPDATE coll_quiz_session_question (is_correct, points_earned, time)
   → UPDATE coll_quiz_session (counters)
3. User finishes quiz
   → engine.py:complete_exam_session()
   → Calculate final score, percentage, pass/fail
   → UPDATE coll_quiz_session (status=completed, scores)
   → engine_advanced.py:_update_user_mastery() → UPSERT eng_user_quiz_topic_mastery
   → engine_advanced.py:_update_user_streak() → INSERT coll_user_streak
   → engine_advanced.py:create_cards_from_session() → INSERT coll_user_card (SRS)
   → quiz_builder.py:award_badge_if_qualified() → INSERT coll_user_quiz_badge
   → engine_advanced.py:update_question_analytics() → UPSERT eng_quiz_question_option_analytics
4. User reviews answers
   → engine.py:get_session_review()
   → Returns all questions + selected options + correct options + source citations


FILES
-----
models.py           — 27 Django models (all managed=False)
engine.py           — Core: start_exam_session, submit_answer, complete_exam_session,
                      get_session_review, get_user_stats, get_user_streak_count
engine_advanced.py  — Advanced: leaderboard, SRS, retry, readiness, badges, analytics,
                      adaptive, calibration, reports, bulk import, practice, history
ai_generator.py     — AI: ingest_book_from_pdf, generate_questions_from_chunk,
                      start_generation_job, get_review_queue, approve/reject_question,
                      create_question_manual, update_question_manual
quiz_builder.py     — Quiz CRUD: create_quiz_with_questions, update_quiz_with_questions,
                      award_badge_if_qualified
embeddings.py       — Semantic: compute_embedding, verify_faithfulness, nli_classify,
                      find_similar_questions, is_duplicate_question
views_api.py        — 25 API endpoints (JSON, login_required)
urls.py             — All API routes under /mastermind/


QUESTION TYPES
--------------
mcq_single    — Single correct answer (auto-graded)
mcq_multi     — Multiple correct answers (auto-graded, all-or-nothing)
true_false    — Two options: সত্য/মিথ্যা (auto-graded)
fill_blank    — User types answer, NFC-normalized comparison (auto-graded)
essay         — Free text, reviewer grades manually (V2)


AI PIPELINE CONFIG
------------------
Ollama:
  URL: http://localhost:11434/api/generate
  Model: llama3.2:3b (2GB, CPU)
  Temperature: 0.3
  num_predict: 1500 (prevents runaway generation)
  Timeout: 600s
  Install: C:\Users\mehfil\AppData\Local\Programs\Ollama\ollama.exe

Embeddings:
  Model: paraphrase-multilingual-MiniLM-L12-v2 (~400MB, Bengali)
  Dimension: 384 float32
  Storage: VARBINARY(MAX) via numpy tobytes()

NLI cross-encoder:
  Model: MoritzLaurer/mDeBERTa-v3-base-xnli-multilingual-nli-2mil7 (~1.5GB)
  Gate: Tier 0 (word-match) → Tier 1 (similarity, reject <0.30) → Tier 2 (NLI entailment ≥0.50)
  NLI always fires if similarity ≥ 0.30 (no auto-pass shortcut — negation pairs proved it unsafe)

NLI scores persisted on coll_question:
  nli_similarity_score, nli_entailment_score, nli_contradiction_score,
  nli_verdict_code, nli_confidence_level_code


CONSUMER INTEGRATION
--------------------
Consumer apps import from mastermind.engine:

    from amolnama_news.site_apps.mastermind.engine import (
        start_exam_session,
        submit_answer,
        complete_exam_session,
        get_session_review,
        get_user_stats,
    )

Consumer apps own their templates/CSS/JS. Mastermind returns dicts/JSON.


KNOWN ISSUES + FIXES
--------------------
1. pyodbc ntext on chunk_text: use raw pyodbc cursor with per-param
   setinputsizes([(SQL_WVARCHAR, 0, 0)]). See troubleshooting.md §5b.
2. Ollama 70% JSON parse failures: add format='json' to Ollama call (pending).
3. NLI name-swap blind spot: mDeBERTa entails single-name substitutions at 0.69.
   Needs named-entity post-check in V2.
4. option_label truncation: derive from index position, never trust LLM output.


FUTURE SCALING — laptop → server
----------------------------------
GPU: auto-detected by Ollama + torch (zero code change).
CPU parallelism: NOT automatic — needs runtime_profile.py module (deferred).
Triggers: GPU available, or >50 book backlog, or gen <30s per call.
Effort: ~2 hours when needed.


CHANGELOG — 2026-04-17 (engine completion batch)
---------------------------------------------------
Added (engine modules):
  - notifications.py    — pulse + messenger DM, no email, no SMTP, no third party.
                          Replaces earlier email-based notifications module.
                          Public API: notify_quiz_results_ready, notify_quiz_creator_permission_granted,
                          notify_ai_generation_completed.
                          Settings: MASTERMIND_SYSTEM_SENDER_PROFILE_ID (optional).
  - exporters.py        — bulk question + quiz export to JSON or CSV.
                          Round-trip-safe with bulk_import_questions.
  - image_uploads.py    — multipart image uploader, validates type/size,
                          returns public URL for question_image_url / option_image_url.
  - certificates.py     — auto-issue pass certificates with unforgeable serials.
                          Browser-native print-to-PDF (no server library needed).
                          Public verify URL: /mastermind/certificate/<serial>/.
  - webhooks.py         — outbound HTTP POST when supported events fire
                          (Zapier / Slack / Discord / n8n / custom).
                          HMAC-SHA256 signature header X-Mastermind-Signature.
                          Soft-fail dispatch on background thread.
  - analytics.py        — read-only aggregation queries for chart dashboards
                          (per_quiz_score_distribution, per_quiz_pass_rate_over_time,
                          per_quiz_question_difficulty, per_user_performance_summary,
                          topic_engagement_overview).

Added (engine functions in existing modules):
  - quiz_builder.py: clone_quiz(), archive_quiz(), delete_quiz(),
                     can_user_manage_quiz() permission helper,
                     _clone_question_with_options() internal.
  - engine.py:       _grade_matching(), _grade_ordering(),
                     _effective_time_limit_minutes() accommodation helper,
                     grant_session_accommodation().
                     submit_answer() now accepts matching_pairs / ordering_option_ids.

Added (DB columns + tables — see notes/sql-script/):
  - exam_proctoring_level DEFAULT flipped 0 → 1 (Phase 2 prep)
  - coll_quiz_session_question.matching_pairs_json / ordering_option_ids_json
  - ref_quiz_question_type seed: 'ordering' (auto-grade), 'essay' (manual grade)
  - coll_quiz_session: session_extra_time_minutes, session_no_time_limit,
                       session_accommodation_notes,
                       link_accommodation_granted_by_user_profile_id
  - coll_quiz: exam_certificate_template_html (NVARCHAR(MAX) NULL)
  - coll_certificate (new table)
  - coll_webhook_subscription (new table)

API endpoints added (all under /mastermind/api/):
  POST exam/<id>/clone/                          — duplicate quiz
  POST exam/<id>/archive/   + unarchive/         — soft-archive
  POST exam/<id>/delete/                         — soft-delete
  GET  exam/<id>/export/                         — single quiz JSON bundle
  GET  question/export/?format=json|csv          — bulk question export
  POST question/upload-image/                    — multipart image upload
  POST session/<id>/accommodation/               — grant extra time
  GET  webhook/subscriptions/  POST create/  POST <id>/delete/
  GET  analytics/quiz/<id>/score-distribution/
  GET  analytics/quiz/<id>/pass-rate/?days=
  GET  analytics/quiz/<id>/question-difficulty/
  GET  analytics/topic/engagement/?days=
  GET  analytics/user/performance/

Public-facing URL added (no auth):
  GET  /mastermind/certificate/<certificate_serial>/

Webhook event codes (consumers subscribe to these):
  quiz_session_completed, quiz_session_passed, quiz_session_failed,
  ai_generation_completed, quiz_creator_permission_granted,
  proctoring_violation_logged.


CHANGELOG — 2026-04-17 (round 3 — gap fill + deferred plans)
-------------------------------------------------------------
Shipped UIs:
  - Webhook subscription manager UI at /quizadmin/webhooks/
  - Accommodation override form on the proctoring session audit page
  - Per-quiz comments / discussion thread on the quiz leaderboard page
    (with reply, like, delete, staff pin)

Shipped engine:
  - mastermind/comments.py — list/create/delete/pin/react helpers
  - 5 new API endpoints under /mastermind/api/quiz/.../comments/

Shipped DB (run notes/sql-script/mastermind-quiz-comments.sql):
  - coll_quiz_comment              — discussion comments + replies
  - coll_quiz_comment_reaction     — likes per (comment, user, type)

Deferred — full written plans under notes/claude/app_documentation/:
  - plan-mastermind-multiplayer.txt          (HIGH priority, 1-2 weeks)
  - plan-mastermind-qti-import-export.txt    (LOW, customer-driven)
  - plan-mastermind-realtime-collab-authoring.txt  (VERY LOW, 4-8 weeks)
  - plan-mastermind-future-extras.txt        (8 small items, ~25 dev-days total)

Nothing on the deferred list blocks current platform usage.


CHANGELOG — 2026-04-17 (round 4 — consumer-side quiz-take UX)
-------------------------------------------------------------
First end-user-facing UI for the engine. Closes the largest functional
gap (engine had no consumer until now).

Reusable partial:
  - mastermind/templates/mastermind/partials/quiz_take.html
  - mastermind/static/mastermind/assets/css/components/quiz-take.css
  - mastermind/static/mastermind/assets/js/quiz-take.js

  Drop into any consumer page that wants to render Mastermind quizzes:
    {% include 'mastermind/partials/quiz_take.html' with quiz_id=quiz.id quiz=quiz %}

  Provides:
    - Welcome screen with quiz metadata (title, total questions, time limit,
      pass mark, proctoring level)
    - Per-question loop (7 of 8 question types supported — matching TODO)
    - Server-driven timer with warning + critical colour states
    - Immediate explanation rendering when exam_show_explanation_code='each_question'
    - Source citation rendered inline (book #, chapter #, page) per CLAUDE.md
      rule 7 — "anti-lying feature"
    - Submit on finish OR auto-submit on time-out
    - Results screen with pass/fail badge, score breakdown, badges earned

  Question type renderers in quiz-take.js:
    mcq_single / true_false   → radio group
    mcq_multi                 → checkbox group
    fill_blank                → text input
    short_answer / essay      → textarea (manual grading)
    ordering                  → up/down arrow list with shuffled initial order
    matching                  → NOT YET (engine API needs match_pairs payload)

  Proctoring auto-boots when exam_proctoring_level >= 1 via
  window.mastermindProctoring.start(). Level 2 webcam consent gate fires
  via the existing proctoring_consent partial.

First consumer page (staff dogfood):
  - /quizadmin/quiz/<id>/take/  (Quizzes list → "Take" row action)
  - quizadmin/views.py:quiz_take_page
  - quizadmin/templates/quizadmin/pages/quiz_take.html (just includes the partial)

Engine endpoints used by quiz-take.js:
  POST /mastermind/api/exam/<id>/start/   → session + all questions
  POST /mastermind/api/exam/<id>/answer/  → grade one answer + explanation
  POST /mastermind/api/exam/<id>/submit/  → finalise score, auto-issue cert,
                                            fire pulse + DM + webhook events

KNOWN LIMITATIONS / FOLLOW-UP TODOS:
  - matching question type display (engine API extension needed)  ✅ shipped 2026-04-17
  - certificate link not yet shown on results screen              ✅ shipped 2026-04-17
  - resume mid-session                                            ✅ shipped 2026-04-17
  - mobile drag-and-drop for ordering (current up/down arrows are fine
    but drag would be nicer)                                       — deferred

Cache bumped to v708.

================================================================
2026-04-17 — Round 5 → Multi-player v1 (this session's deltas)
================================================================

CONSUMER WIRING
  historybd        → /itihas/quiz/  + /itihas/quiz/<id>/take/
  constitutionbd   → /songbidhan/quiz/  + /songbidhan/quiz/<id>/take/
  Both reuse the shared mastermind/partials/quiz_take.html partial.
  Adding a new history / constitution quiz now needs ZERO code change here:
  publish in Quiz Panel with topic "Bangladesh History" or
  "Bangladesh Constitution" and it appears automatically.

CLOSED ROUND-5 GAPS
  - Matching question type (8/8 types now end-to-end)
    * engine.py emits match_stems + match_responses (responses shuffled
      when exam_shuffle_options=True)
    * quiz-take.js + lobby-player.js render one <select> per stem
    * submit blocks until every stem has a chosen response
  - Certificate link on results
    * GET /mastermind/api/session/<id>/certificate/ (owner-only)
    * Take page polls 3× × 1.5s on results screen and unhides the
      "View certificate" link if the engine issued one
  - Resume mid-session
    * GET /mastermind/api/exam/<id>/resume-check/  → in_progress_session_id
    * POST /mastermind/api/session/<id>/resume/    → snapshot + already_answered hints
    * Welcome screen auto-detects + shows "Resume attempt" alongside Start

FLAG-A-QUESTION (backlog item #5 — DONE)
  Student side: 🚩 collapsible panel on the take-page explanation block.
  Six reason codes + optional 500-char note. Auto-archive at 3 pending.
  Staff side: /quizadmin/flagged/  (added "Flagged" tab in nav).
  Status filter (pending / resolved / invalid / all). Inline Resolve / Reject.
  APIs: existing POST /mastermind/api/question/report/ + new
        GET  /mastermind/api/question/report/list/?status=...
        POST /mastermind/api/question/report/<id>/review/  body {action: resolve|reject}

GRADEBOOK CSV + FULL BACKUP (backlog #7 + #8 — DONE)
  GET /mastermind/api/exam/<id>/gradebook.csv  (?include_in_progress=1)
    One row per session: scores, times, pass/fail, threshold. UTF-8 BOM.
    "⬇ Download gradebook CSV" button on the leaderboard page.
  GET /mastermind/api/admin/full-backup/  (staff only)
    Streams every topic, book, chapter, question (with options + match-pairs
    + resolved topic_code/difficulty_code), and quiz (with pool entries).
    Excludes run-state. Round-trip safe with bulk_import_questions.

MULTI-PLAYER v1 (Kahoot/Quizizz-style live group quizzes — SHIPPED)
  DB:
    notes/sql-script/mastermind-multiplayer-lobby.sql  (idempotent)
    [mastermind].[coll_quiz_lobby]         — one row per game room
    [mastermind].[coll_quiz_lobby_player]  — one row per joined player
    [mastermind].[coll_quiz_lobby_event]   — append-only audit log
    coll_quiz.exam_lobby_speed_bonus_enabled / _streak_bonus_enabled
  Engine (mastermind/lobby.py):
    create_lobby / join_lobby / leave_lobby / mark_ready / start_lobby /
    advance_question / submit_lobby_answer / finalise_lobby / get_lobby_state
    + Kahoot-style speed bonus (1 - elapsed/max × 0.5 multiplier on correct)
    + Streak bonus (+10% per consecutive correct, capped +50%)
    + Late-answer / wrong-index / duplicate-answer protection
    + Idempotent advance via expected_index check
  Transport (mastermind/consumers_lobby.LobbyConsumer):
    /ws/mastermind/lobby/<lobby_id>/  via routing.py + asgi.py
    Group: lobby_<lobby_id>
    Inbound: ready / leave / start_lobby / advance_question /
             submit_answer / request_state
    Personal answer_result + group broadcast_state on every state change
  HTTP API (UI bootstrap before WS connects):
    POST /mastermind/api/lobby/create/   {quiz_id, mode_code, max_players, question_seconds}
    POST /mastermind/api/lobby/join/     {join_code}
    GET  /mastermind/api/lobby/<id>/
    GET  /mastermind/api/lobby/by-code/<code>/
  Player UI:
    /play/<join_code>/  → /mastermind/play/<code>/  (login required)
    All 8 question types render in lobby mode.
    JSON island via {{ lobby_state|json_script:"..." }} (XSS-safe).
    Sticky live leaderboard, gold/silver/bronze podium gradients,
    server-driven countdown timer.
  Host UI:
    /quizadmin/quiz/<id>/host/  (added "🎮 Host" button on quiz_list)
    Auto-creates a waiting lobby (re-uses existing).
    Big join code + share URL + Copy button + ready counts +
    Start/Next-question controls + final podium.
  Webhooks (added to webhooks.SUPPORTED_EVENT_CODES):
    lobby_started + lobby_completed events fire via existing subscriptions

POLISH PASS
  All today's CSS upgraded with:
    - hover lift (transform: translateY) on cards + buttons + leaderboard rows
    - gold/silver/bronze podium gradients on top-3 leaderboard rows
    - join-code visual: monospace 4xl with accent gradient + dashed accent border
    - quiz cards: 4px accent slide-in bar on hover + CTA arrow shift
    - flagged-questions table: mobile reflow to card stack at ≤720px (data-label)
    - resume banner: warning gradient + 4px left bar
    - All transitions wrapped in @media (prefers-reduced-motion: reduce)
    - Mobile breakpoints tightened: ≤480px font drops, ≤768px sidebar collapses,
      ≤1024px tablet sidebar narrows

NAMING CONVENTION CLEANUP
  - JS: short loop vars (i, j, n, tmp, raw, chosen, msg, err) renamed to
    descriptive forms (sourceIndex, swapIndex, swapBuffer, helperDiv,
    serverMessage, parseError, selectedResponseByStem, etc.)
  - Python (lobby.py _grade_lobby_answer): raw → raw_selected_option_id,
    chosen → selected_option_ids, pairs → matching_pairs, entry → pair_entry,
    student_order/canonical → *_ordered_option_ids
  - HTML: 3 anchors missing id+name now have them
    (historybd-quiz-take back link, constitutionbd-quiz-take back link,
    flagged-questions question-edit link)

BUG-SCAN FIXES
  - XSS hardening: result.new_badges escaped; lobby JSON islands switched
    to Django json_script filter (auto-escapes </script> in display_name)
  - Logic bug: lobby duplicate-answer detection used substring match that
    falsely matched 1 inside 10/11/12 — anchored with trailing comma
  - Dead code removed: unused CollQuizLobby import in consumers_lobby.py
  - Accessibility: JS-rendered radios/checkboxes in lobby-player now get
    unique inputId + label.htmlFor

SQL SCRIPT RUN ORDER (this session)
  notes/sql-script/mastermind-multiplayer-lobby.sql   — REQUIRED for multi-player

10/10 POLISH PASS
  Sound (mastermind/static/mastermind/assets/js/lobby-sfx.js — new module):
    Web Audio API synth — zero external files. 8 events:
    question_advance, lock_in, correct, wrong, time_warning, time_up,
    lobby_start, game_over. Lazy AudioContext init. Honours
    prefers-reduced-motion + localStorage("mastermind_lobby_sfx").
    150ms per-event debounce. Single source of truth for the
    🔊 / 🔇 toggle wiring via wireToggleButton(buttonElement) — both
    lobby-player.js and lobby-host.js call it with one line each
    (no duplicate 18-line wiring code).
  Animations (lobby.css keyframes):
    - mastermind-lobby-question-enter (320ms slide-up + fade)
    - mastermind-lobby-timer-pulse (1.2s warning ≤10s, 0.6s critical ≤5s)
    - mastermind-lobby-status-flash (240ms scale pop on answer feedback)
    - mastermind-lobby-podium-drop (staggered drop, top-3 reveal last)
    - mastermind-lobby-confetti-spin (🎉 emoji rotates above #1 row)
    All gated behind @media (prefers-reduced-motion: reduce).
  Progress bar:
    - 4px gradient track shows fraction complete across all questions
    - role="progressbar" + aria-valuenow updated on every advance
    - Smooth 320ms width transition with reduced-motion override
    - Same component on both player + host pages (single CSS class)
  Keyboard shortcuts (player only):
    - 1-9 picks the Nth radio option
    - Enter submits when an answer is locked
    - Skipped when typing in input/textarea/select
  Floating SFX toggle on host page:
    - Desktop: absolute top-right of lobby grid
    - Mobile (≤768px): fixed bottom-right of viewport (avoids share-code overlap)
    - Elevation-3 shadow on mobile for legibility above scrolling content

Cache bumped to v717.
